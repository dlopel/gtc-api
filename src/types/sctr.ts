interface SctrStorage {
    id: string
    pensionNumber: string
    healthNumber: string
    dateStart: string
    dateEnd: string
    insuranceCompany: string
    imagePath: string
    observation: string | null
}

export default SctrStorage